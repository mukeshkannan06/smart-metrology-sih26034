# ==============================================================================
# SMART METROLOGY — SIH26034 FRONTEND ENVIRONMENT CONFIGURATION
# ==============================================================================
# IMPORTANT: All variables prefixed with VITE_ are embedded into public bundles.
# NEVER put server secrets, database URIs, or Gemini API keys here.

# --- BACKEND API TARGET URL ---
# Local Development: Leave blank or set to http://localhost:5000 (handled via Vite proxy)
# Production on Vercel: Set to your live Render backend URL (e.g., https://smart-metrology-api.onrender.com)
VITE_API_BASE_URL=http://localhost:5000
